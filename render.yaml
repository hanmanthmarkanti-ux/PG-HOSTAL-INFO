services:
  - type: web
    name: pg-hostel-api
    runtime: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm run seed && npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 5000
